{{#each groups}}
  <dt>{{year}}</dt>
  {{#each blogs}}
    <dd>
      <a href="/blog/{{attributes.filename}}/">{{attributes.title}}</a>
      <small><time>{{attributes.displayDate}}</time></small>
    </dd>
  {{/each}}
{{/each}}
